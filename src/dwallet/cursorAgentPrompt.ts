/**
 * Paste into Cursor Chat (Agent). @-mention AGENTS.md so the agent loads workflow + env.
 */
export const DWALLET_CURSOR_AGENT_PROMPT = `I'm in the Harmony Heart Beats / PRISM app on the dWallet tab (Operator console).

@AGENTS.md

Walk me through the next steps for my situation: I use Solana devnet and want to align prism CLI (create → init_prism → sign or sign --gated) with what I paste into the Operator console (CPI PDA, dWallet inspect, MessageApproval PDA, poll status). Give ordered steps and what to copy where.`;

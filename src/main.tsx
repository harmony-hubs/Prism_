import React from 'react';
import ReactDOM from 'react-dom/client';
import { HarmonyDashboard } from './app';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HarmonyDashboard />
  </React.StrictMode>
);

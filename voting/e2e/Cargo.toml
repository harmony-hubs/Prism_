[package]
name = "hollow-voting-e2e"
version = "0.1.0"
edition = "2024"

[dependencies]
solana-sdk = "2"
solana-client = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
clap = { version = "4", features = ["derive"] }
